<Controls:MetroWindow x:Class="VenPiece.Desktop.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:local="clr-namespace:VenPiece.Desktop.Views"
        mc:Ignorable="d"
        Title="Login" 
        Icon="../Media/VenPiece.png"
        Height="435" Width="650"
        ShowIconOnTitleBar="True">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="64*"/>
            <ColumnDefinition Width="45*"/>
            <ColumnDefinition Width="64*"/>
        </Grid.ColumnDefinitions>
        <Border BorderThickness="145,1" CornerRadius="50" Grid.ColumnSpan="3" Background="#bd0f66">
        </Border>

        <Grid Background="#bd0f66" Grid.ColumnSpan="3" Margin="171,10,171,9" >

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto" MinHeight="177"/>
                <RowDefinition Height="Auto" MinHeight="3"/>
            </Grid.RowDefinitions>
            <Image Name="Logo" Margin="0,0,0,189" Grid.RowSpan="2" />
            <StackPanel Grid.Row="1" Orientation="Horizontal" Height="126" VerticalAlignment="Bottom">
                <TextBox HorizontalAlignment="Stretch"
                     Height="Auto" 
                     Margin="60,100,0,0" 
                     Width="175" Background="{x:Null}"
                     TextChanged="TextBox_TextChanged"
                     Controls:TextBoxHelper.Watermark="User Name"
                         >
                    <TextBox.Effect>
                        <DropShadowEffect/>
                    </TextBox.Effect>
                </TextBox>

            </StackPanel>
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,126,0,0" Grid.RowSpan="2">

                <PasswordBox HorizontalAlignment="Stretch"
                     Height="Auto" 
                     Margin="60,100,0,0" 
                     Width="175" Background="{x:Null}"
                     Grid.Column="1"
                     Controls:TextBoxHelper.Watermark="Password"
                     Controls:ControlsHelper.CornerRadius="10"
                             >
                    <PasswordBox.Effect>
                        <DropShadowEffect/>
                    </PasswordBox.Effect>
                </PasswordBox>


            </StackPanel>
            <Button Name="Login"
                Content="Login" 
                HorizontalAlignment="Stretch" Margin="112,75" 
                Grid.Row="3" Background="{x:Null}"
                Click="Login_Click"
                Controls:ButtonHelper.CornerRadius="10" Cursor="Hand"
                >
                <Button.Effect>
                    <DropShadowEffect/>
                </Button.Effect>
            </Button>
            <Image x:Name="Logo1"
                   HorizontalAlignment="Stretch"
                   Height="83"
                   Margin="96,24,96,0"
                   Grid.RowSpan="2" 
                   VerticalAlignment="Top"
                   Source="../Media/666201 (1).png"
                   
                  />
        </Grid>
    </Grid>
</Controls:MetroWindow>

